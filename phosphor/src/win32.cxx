#include <Windows.h>
#include <cstdlib>

auto WINAPI wWinMain(HINSTANCE /* hinstance */,
                     HINSTANCE /* hprevinstance */,
                     PWSTR /* pcmdline */,
                     int /* ncmdshow */) -> int {
    return EXIT_SUCCESS;
}
